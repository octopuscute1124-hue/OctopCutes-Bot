# :octopus: OctopCutes 防護機器人

一個專為 Discord 設計的全方位安全防護機器人，具備行為型攻擊偵測、全域黑名單同步、警報系統與可視化控制面板。

> 本版本已修復多項容易誤封（false positive）與稽核缺口的問題，詳見文末[更新日誌](#pencil-更新日誌)。

---

## :sparkles: 功能特色

- :shield: **22 項安全防護**：涵蓋洪水攻擊、腳本/SelfBot、Webhook 濫用、權限變更、XSS 注入、惡意檔案、撞庫攻擊等
- :warning: **累犯制**：內容型偵測（XSS、惡意 Rich Presence）改為「首次警告、再犯才封鎖」，降低誤判造成的永久傷害
- :clipboard: **全域黑名單**：JSON 儲存，跨伺服器同步封鎖，並記錄來源伺服器、原因、時間以利稽核
- :outbox_tray: **伺服器可退出全域黑名單**：不想被其他伺服器的誤判連累，可個別關閉
- :video_game: **直覺操作**：`!章魚` 叫出控制面板，按鈕切換設定，安全開關支援分頁
- :bar_chart: **黑名單查詢**：一鍵查看所有被封鎖帳號
- :arrows_counterclockwise: **定期掃描**：每 30 分鐘自動掃描所有伺服器
- :floppy_disk: **設定持久化**：重啟機器人保護設定不消失
- :page_facing_up: **操作日誌**：所有自動封鎖與管理員操作都會寫入 `logs.json`

---

## :rocket: 快速開始

### 安裝

```bash
git clone https://github.com/octopuscute1124-hue/OctopCutes-Bot.git
cd OctopCutes-Bot
npm install
```

首次啟動會自動建立 `config.json`／`blacklist.json`／`logs.json`（執行期資料，不納入版本控制，可參考同目錄的 `*.example.json` 範本）。

需要 [Node.js](https://nodejs.org/) 18 以上版本，以及 `discord.js` v14。

### 設定

1. 複製 `.env.example` 為 `.env`
2. 填入你的 Discord Bot Token 與開發者（機器人擁有者）ID

```env
DISCORD_TOKEN=你的機器人Token
DEVELOPER_ID=你的Discord使用者ID
```

3. 到 [Discord Developer Portal](https://discord.com/developers/applications) 為機器人開啟以下 **Privileged Gateway Intents**：
   - `SERVER MEMBERS INTENT`
   - `MESSAGE CONTENT INTENT`
   - `PRESENCE INTENT`
4. 邀請機器人加入伺服器時，至少給予 `Ban Members`、`Manage Channels`、`Manage Roles`、`Manage Webhooks`、`Moderate Members`（禁言）等權限。

### 啟動

```bash
node bot.js
```

Windows 使用者也可以直接執行附帶的 `start.bat`（請先修改內部路徑為你自己的專案位置）。

---

## :book: 指令說明

| 指令 | 說明 | 權限 |
|------|------|------|
| `!章魚` | 開啟控制面板 | 管理員，短時間內重複開啟會被冷卻限制（`bruteForce` 開關） |

面板內按鈕：

| 按鈕 | 說明 | 權限 |
|------|------|------|
| 🛡️ 安全設定 | 切換 22 項安全防護（分兩頁） | 管理員 |
| 🔔 警報設定 | 切換 9 項警報通知、設定警報頻道 | 管理員 |
| 🔄 刷新 | 重新整理面板數據 | 管理員 |
| 📋 黑名單 | 查看全域黑名單 | 僅開發者 |
| 👑 白名單 | 管理不受保護的使用者/角色 | 僅開發者 |
| 💬 自動回應 | 設定關鍵字自動回覆 | 僅開發者 |
| 📤 匯出 | 匯出該伺服器目前的設定 JSON | 僅開發者 |

---

## :shield: 安全防護一覽（22 項）

| 開關 | 說明 | 觸發後動作 |
|---|---|---|
| `stopLoss` 止損 | 短時間內在多頻道 @everyone/@here | 立即封鎖 |
| `mentionSpeed` @mention 速度 | 單則訊息 ≥3 個提及且速度異常 | 立即封鎖 |
| `scriptDetection` 腳本 | 發訊速度異常規律（疑似機器人） | 立即封鎖 |
| `voiceAbuse` 語音濫用 | 快速切換語音頻道 | 立即封鎖 |
| `webhookMonitor` Webhook | 短時間內大量建立/發送 Webhook | 自動刪除 Webhook |
| `selfbotDetection` SelfBot | 回應速度過快 | 立即封鎖 |
| `floodProtection` 洪水 | 極短時間大量發送訊息 | 立即封鎖 |
| `floodJoin` 洪水加入 | 短時間大量成員加入 | 發送警報 |
| `permissionSpam` 權限變更 | 頻道權限被異常快速修改 | 發送警報 |
| `maliciousFile` 惡意檔案 | 上傳 `.exe .scr .bat .cmd .com .pif .vbs` | 刪訊息+立即封鎖 |
| `xssProtection` XSS | 訊息含真正的注入樣式（略過程式碼區塊） | 首犯警告，累犯封鎖 |
| `richPresence` RichP | 個人狀態文字含注入樣式 | 首犯警告，累犯封鎖 |
| `crawlerDetection` 爬蟲 | 大量請求行為 | 禁言 60 秒 |
| `collusionAttack` 撞庫 | 短時間內反覆嘗試加入 | 立即封鎖 |
| `suspiciousAccount` 可疑帳號 | 帳號 <7天 或 <30天無頭像 | 僅發送警報（不自動封鎖） |
| `bruteForce` 面板冷卻 | 短時間重複開啟 `!章魚` 面板 | 暫時限制使用 |
| `rateLimit` RateLimit | 控制面板操作過於頻繁 | 暫時拒絕操作 |
| `autoDegrade` 自動降級 | 系統負載過高或行為異常 | 暫時進入保護模式 |
| `inviteMonitor` 邀請濫用 | 短時間內大量建立邀請連結 | 立即封鎖 |
| `roleLock` 角色鎖定 | 非管理員角色被賦予管理員權限 | 自動復原權限+發送警報 |
| `channelSpam` 頻道監控 | 大量建立/刪除頻道 | 發送警報 |
| `honorGlobalBlacklist`* 全域黑名單套用 | 是否套用其他伺服器造成的封鎖 | 關閉後該伺服器只保留自己觸發的封鎖 |

\* 因 Discord 單則訊息最多 5 列元件、面板按鈕已滿，此項目目前只能透過直接編輯 `config.json` 的
`securitySettings.<伺服器ID>.honorGlobalBlacklist` 設為 `false` 來關閉，尚未加入面板按鈕。

以上除 `honorGlobalBlacklist` 外皆可在「🛡️ 安全設定」面板中即時切換（分三頁顯示）。

---

## :file_folder: 檔案說明

| 檔案 | 說明 |
|------|------|
| `bot.js` | 主程式 |
| `blacklist.json` | 全域黑名單（**執行期自動產生**，不納入版本控制，可參考 `blacklist.example.json`） |
| `config.json` | 各伺服器設定（**執行期自動產生**，不納入版本控制，可參考 `config.example.json`） |
| `logs.json` | 操作日誌（**執行期自動產生**，不納入版本控制，保留最近30天） |
| `crash.log` | 當機／未處理錯誤紀錄（V0.2.3 新增，供追查異常重啟原因） |
| `config.example.json` | 設定檔範本（V0.2.4 新增） |
| `blacklist.example.json` | 黑名單範本（V0.2.4 新增） |
| `.env` | 環境變數（Token、開發者ID），**請勿上傳到公開倉庫** |
| `PRIVACY.md` | 隱私條款 |
| `start.bat` | Windows 一鍵啟動腳本 |

---

## :bar_chart: 資料生命週期

- 黑名單：只要伺服器把 `honorGlobalBlacklist` 設為 `false`，就不會被其他伺服器造成的封鎖影響。
- 日誌：`logs.json` 會自動清除 30 天前的紀錄，且上限 5000 筆。
- 設定：儲存於 `config.json`，重啟機器人不會遺失（與黑名單一樣落地為檔案，非純記憶體）。

---

## :pencil: 更新日誌

### V0.4.8（2026-09-10）— 處置可撤銷・一鍵還原

> 無論防護多強都可能誤判——所以**任何自動處置都必須可以取消**。

#### ↩️ 一鍵撤銷
- 每次機器人自動 **Ban／禁言** 都會寫入處置歷史（`actionHistory`，存於黑名單檔、跨重啟持久化、上限 200 筆）
- 主面板新增 **「↩️ 處置撤銷」** 按鈕——列出該伺服器最近 8 筆自動處置（成員／原因／時間／是否已撤銷）
- 管理員點按鈕即可撤銷：**Ban → 解封**、**禁言 → 解除**——誤判 10 秒內補救

#### 🛡️ 撤銷保護（放過就是放過）
- 處置被管理員撤銷後，**24 小時內同一成員不會再被機器人自動處置**（只警告並標註「先前已被撤銷」）
- 防止機器人剛被糾正又立刻重複處置同一人
- 撤銷動作寫入稽核日誌（`UNDO_PUNISHMENT`：由誰撤銷、撤銷什麼）

#### 🧪 測試
- 新增 tests/v048Undo.test.js（撤銷保護 5 項＋撤銷執行 3 項＋原始碼整合斷言 5 項 共 13 項）
- **27 套件全部通過**（v048 納入 npm test）

---

### V0.4.7（2026-09-09）— 智慧裁定・攻擊者圖譜

#### 🕵️ 攻擊者圖譜（跨伺服器認人）
- 詐騙/惡意訊號命中時，把「發送者 → 詐騙域名」寫入圖譜（`attackGraph`，存於黑名單檔、跨重啟持久化）
- **同一個使用者發過 ≥2 個不同詐騙域名 → 判定攻擊者**（換域名、換伺服器也認得）
- 3+ 域名 → 嚴重慣犯——處置直接升級，不再給「初犯寬容」

#### ⚖️ 智慧裁定（不再只看當下分數）
處置層級由三項因素動態調整（每項都有防誤傷考量）：
1. **圖譜慣犯**：2 域名 warn→timeout；3+ 域名直接 ban
2. **伺服器信譽**：低信譽（<0.35）伺服器對詐騙的首次處置**降一級**（避免低信譽帶風向誤傷成員）——但攻擊者不受保護
3. **學習品質**：機器人處在 strict 校準（誤學率高→謹慎）時，詐騙類處置**升一級**
- 中性環境（無圖譜/中性信譽/normal）行為與舊版完全一致——向後相容
- 裁定附「理由註記」（圖譜 X 域名／低信譽降級／嚴格升級），處置訊息與日誌透明可查

#### 🧪 測試
- 新增 tests/v047Adjudicate.test.js（圖譜 10 項＋中性裁定 3 項＋慣犯升級 3 項＋信譽降級 4 項＋strict 升級 2 項＋非詐騙不受影響 2 項 共 24 項）
- **26 套件全部通過**（v047 納入 npm test）

---

### V0.4.6（2026-09-09）— 檔案特徵碼掃描（進階靜態偵測）

#### 🛡️ 附件內容特徵掃描（不只擋副檔名，直接看內容）
- 每則訊息的附件自動下載掃描（每訊息 ≤3 個、單檔 ≤2MB、每 30 秒全域 ≤10 個——防 DoS）
- **PE 可執行檔頭偵測**：MZ 頭＋PE 標記解析（真可執行檔，非只看副檔名）
- **危險副檔名＋雙副檔名偽裝**：`photo.jpg.exe`、`.scr`、`.hta` 等
- **PowerShell 編碼執行**（`-enc`）、**LOLBin 下載執行鏈**（certutil/bitsadmin/mshta/rundll32 等）
- **混淆執行**（`eval(atob(`、`new Function`）、**Base64 巨塊載入**
- **勒索軟體特徵**（加密通知＋加密貨幣錢包字串）
- **內嵌 URL 抽取**：掃出檔案內藏的任何連結，與詐騙黑名單＋仿冒品牌比對

#### ⚖️ 風險計分與處置
- 每命中一項加權（PE 3、混淆 3、副檔名 2、Base64 2…）
- 風險 ≥4 → 🚨 高風險檔案；否則 ⚠️ 可疑檔案——皆警示＋記 `FILE_SCAN` 日誌
- **不自動封鎖**（保持漸進處置）——但命中會進行為風險評分體系

#### 🧪 測試
- 新增 tests/v046FileScan.test.js（PE 頭 2＋正常不誤報 3＋副檔名 2＋腳本/下載/混淆/勒索 4＋URL 抽取 1＋風險加總 3 共 15 項）
- **25 套件全部通過**（v046 納入 npm test）

---

### V0.4.5（2026-09-09）— 來源信譽・伺服器可信度

#### 🏆 伺服器信譽制度（讓舉報品質說話）
- 每個伺服器有信譽分（預設 0.5 中性，範圍 0~1）
- **舉報命中**（該伺服器舉報的域名最終被證實為詐騙）→ 信譽 +0.1
- **被管理者駁回**（誤學）→ 信譽 -0.2（面板駁回與低齡審核駁回皆計）
- 學習提升時把舉報來源記錄進域名 meta（`sources`），供後續追責

#### ⚖️ 信譽權重計分（不再盲目按次數行事）
- 候選計分乘上來源信譽權重：`權重 = 1 + (信譽 - 0.5)`（介於 0.5~1.5）
- 高信譽伺服器（0.8）每次舉報 +1.3——可信來源更快促成共識
- 低信譽伺服器（0.2）每次僅 +0.7——**誤報慣犯難以單獨帶風向**，需高信譽/中性來源背書
- 所有加權機制（跨伺服器、新帳號、仿冒官方、多人中招）都受來源可信度影響
- 中性伺服器權重 = 1 → 既有行為完全向後相容

#### 🧪 測試
- 新增 tests/v045Reputation.test.js（權重計分 7 項＋學習加分 4 項＋駁回懲罰 3 項 共 14 項）
- 適配 v028/v035/v036/v039/v041/v044 共 6 個 record 測試（併入 getGuildReputation 定義）
- **24 套件全部通過**（v045 納入 npm test）

---

### V0.4.4（2026-09-09）— 自我校準・動態學習門檻

#### 🔧 學習品質自我校準（機器人自己調整自己的嚴格程度）
- 新增 `assessLearningTier()`：依近 30 天「提升 vs 駁回」計算學習品質等級
  - 駁回率 ≤10% → **normal**（標準門檻）｜10~30% → **tight**（收緊）｜>30% → **strict**（嚴格）
  - 樣本 <3 筆不調整（避免小樣本誤判）
- **動態學習門檻**：誤學越多，門檻自動越嚴
  - normal：多來源 ≥3／單一 ≥5
  - tight：多來源 ≥4／單一 ≥6
  - strict：多來源 ≥5／單一 ≥8
- **爆發式即時提升也同步校準**（strict 需 5 次連擊、tight 需 4 次、normal 需 3 次）
- 每日彙報新增「🔧 學習門檻」欄位——管理者直接看到機器人目前的自我校準狀態

#### 🧪 測試
- 新增 tests/v044Calibrate.test.js（品質評估 5 項＋每日動態門檻 8 項＋爆發動態門檻 2 項 共 15 項）
- 適配 v027/v028/v035/v036/v038/v039/v040/v041/v042/v043 共 10 個測試（併入 assessLearningTier 定義）
- **23 套件全部通過**（v044 納入 npm test）

---

### V0.4.3（2026-09-09）— 一鍵審核回報・學習品質指標

#### ✅ 低齡警示一鍵審核（人機協同閉環）
- 低齡帳號發送可疑連結的警示 embed 附兩個按鈕：**「✅ 確認釣魚」／「❌ 放行」**（僅管理員可操作）
- 確認釣魚 → 立即加入黑名單（source=管理員審核，記審核者）＋清除觀察池
- 放行 → 自動記錄駁回（7 天防反彈）＋清除候選＋清除觀察池——一鍵完成原本要在面板操作多步的事
- customId 用 Map 對應（token → 域名），繞過 Discord 100 字元 customId 限制

#### 📊 學習品質指標（機器人自我校準可見）
- 每日彙報新增「近 30 天學習品質」欄位：提升數 vs 駁回數＋駁回率
- 管理者一眼看到機器人的學習精準度——駁回率越高代表誤學越多，可考慮調緊學習門檻

#### 🧪 測試
- 新增 tests/v043Review.test.js（審核執行 11 項＋品質指標 6 項 共 22 項）
- 適配 v042（lowAgeScamAlert 加審核按鈕後補注入）
- **22 套件全部通過**（v043 納入 npm test）

---

### V0.4.2（2026-09-09）— 低齡帳號先行警示・彙報再現標記

#### ⚠️ 低齡帳號先行警示
- 新帳號（<7 天）發送**候選詐騙連結**（未證實為詐騙的仿冒域名）時——**不刪除、不封鎖**（避免誤傷新使用者）
- 改為：警報頻道推送「低齡帳號發送可疑連結」審核通知（含帳號、域名、共識人數）＋正常建立候選學習記錄
- 管理員可即時評估：這是釣魚新帳號還是誤發——決定是否介入

#### 🔁 彙報併入「曾被駁回再現」標記
- 每日學習彙報的近 24h 即時封鎖清單，**曾被管理者駁回卻再次被提升**的域名會標記「・曾被駁回再現」
- 管理者直接在彙報看到：哪些域名是「機器學回來的舊案」——不需翻日誌

#### 🧪 測試
- 新增 tests/v042YoungReport.test.js（低齡警示 7 項＋彙報標記 5 項 共 17 項）
- **21 套件全部通過**（v042 納入 npm test）

---

### V0.4.1（2026-09-09）— 人工駁回・誤學矯正（防反彈記憶）

#### ⚖️ 管理者裁定優先於機器學習
- 管理員從詐騙域名面板**刪除**域名 = **駁回**——記錄 rejectedDomains（誰、何時、哪個伺服器）
- 被駁回的域名 **7 天內不再被自動提升**（無論即時爆發或多來源共識）——人工裁定優先，機器不會「學回來」
- 駁回冷卻中若再度被命中，候選會先清掉——等冷卻過後重新觀察，避免反覆誤升

#### 🔁 過冷卻再現警示
- 曾被駁回的域名 **7 天後再次達標**（多來源/持續大量命中）→ 提升成功但寫入 SCAM_REJECTED_AGAIN 事件
- 這代表它可能是**新型態攻擊**或**持續誤判**——管理者可從彙報/日誌看到，決定是否再次駁回

#### 🧪 測試
- 新增 tests/v041Reject.test.js（防反彈 7 項＋每日回合防反彈 5 項 共 17 項）
- **20 套件全部通過**（v041 納入 npm test）

---

### V0.4.0（2026-09-09）— 每日學習彙報・即時封鎖透明度

#### 📡 學習彙報推送
- 每日學習回合若有變動（新增/移除/老化/降級），自動向**所有伺服器的警報頻道**推送「🧠 學習彙報（24h）」摘要
- 內容含：各類變動數量 ＋ **近 24h 爆發式即時封鎖清單**（域名＋命中數＋是否多來源確認）
- 無變動不推送——不會打擾管理員

#### 👀 即時封鎖透明度
- 機器人爆發式封鎖的詐騙域名，管理者 24 小時內看得到（不再只存在於內部日誌）
- 學習系統從「背景黑箱」變成「可監督、可審計」——管理者能確認機器人學的對不對

#### 🧪 測試
- 新增 tests/v040Report.test.js（彙報推送 11 項）
- **19 套件全部通過**（v040 納入 npm test）

---

### V0.3.9（2026-09-09）— 爆發式即時提升・低齡警示標記・學習摘要

#### ⚡ 爆發式即時提升（學習延遲 24h → 即時）
- 釣魚域名在爆發期（短時間多伺服器/多人中招）不再等每日學習回合——**多來源共識且 count≥3 立即升級**為正式詐騙域名
- 官方域名與子域名尾綴（discord.com.evil.xyz 這類）不會被誤擋，仍可正常學習
- 已存在黑名單的域名不重複加入；即時提升寫入 SCAM_LEARN（immediate）事件

#### ⚠️ 低齡警示標記
- 低信任帳號（<7 天新帳號）收到警告時，理由自動附註「新帳號，低信任」；高信任成員附註「高信任成員」
- 管理員與旁觀成員一眼看出警示對象的信任等級

#### 📊 學習摘要日誌
- 每日學習回合若產生變動（提升/移除/老化/降級），寫入 SCAM_SUMMARY 事件——管理員可查詢機器人每回合學了什麼

#### 🧪 測試
- 新增 tests/v039Immediate.test.js（爆發提升 8 項＋摘要 2 項 共 18 項）
- 適配 v035/v036（跨伺服器共識觸發即時提升的新語義）、v028（補注入）
- **18 套件全部通過**（v039 納入 npm test）

---

### V0.3.8（2026-09-09）— 來源多樣性門檻・提升後二次確認

#### 🚪 來源多樣性提升門檻（防單一伺服器灌水誤學）
- **多伺服器共識**（sources≥2）：命中 ≥3 次即提升為正式詐騙域名——多個獨立社群都中招＝高可信
- **單一來源**（只有 1 個伺服器）：需命中 ≥5 次才提升——避免單一伺服器的習慣性誤報（如常用外部連結）污染全域黑名單
- 提升時記錄 confirmed（是否多來源確認）

#### 🔁 提升後二次確認（降級機制）
- **單一來源提升者**進入 24 小時觀察期：期間若**無任何新命中**（hits=0）→ **降級回候選池**（不被正式黑名單誤傷）
- 多來源（confirmed）已具跨伺服器共識，不需二次確認
- 降級時自動恢復候選記錄並寫入 SCAM_UNCONFIRMED 事件——學習系統會「認錯」

#### 🧪 測試
- 新增 tests/v038SourceGate.test.js（門檻 4 項＋二次確認 4 組 共 22 項）
- 適配 v027（達標候選補多來源 sources，維持多伺服器確認語義）
- **17 套件全部通過**（v038 納入 npm test）

---

### V0.3.7（2026-09-09）— 確認制學習・群組撞庫偵測・記憶衰退

#### 👥 確認制學習（10 分鐘群組共識）
- 連結觀察池升級：同一可疑連結在 **10 分鐘窗內**被 ≥2 個「不同使用者」發送才構成群組共識
- 過期使用者自動淡出（10 分鐘沒再發＝不同事件）——避免「昨天有人發過」被誤當成當下共識
- **低齡感知**：低齡帳號（<7 天）參與者單獨統計——撞庫群常用新號，低齡參與越多越可疑

#### 🚨 群組撞庫（collusion）即時處置
- 多人發送同一仿冒連結達成共識 → 升為 **collusion 重訊號**（權重 4，最重一級）
- 處置訊息標明「已由 N 個使用者發送（含 M 個新帳號）」——管理員一眼看懂是群組攻擊而非單一誤發
- 仍走漸進制（warn → 2 次封鎖），不盲目直 ban

#### 🧠 候選記憶衰退（確認制學習的反向）
- **24 小時無新命中**的詐騙候選熱度減半（最低保留 1）——誤學的域名自然淡出，不盲目維持高可信
- 72 小時無命中仍移除（原有老化）——衰退但未移除會觸發保存
- 效果：學習池「活」起來——只有持續被證實的域名維持高權重

#### 🧪 測試
- 新增 tests/v037ConfirmLearn.test.js（時間窗共識 11 項＋記憶衰退 6 項 共 17 項）
- 適配 v036（watchLink 區塊註解字樣更新）
- **16 套件全部通過**（v037 納入 npm test）

---

### V0.3.6（2026-09-09）— 信任分層・Raid 精準偵測・跨使用者共識・混淆還原

#### 🛡️ 信任分層（同一行為依信任度加權）
- **高信任（0.6 倍）**：管理員與白名單——誤傷風險降到最低
- **一般（1.0 倍）**：正常成員
- **低信任（1.3 倍）**：低齡帳號（<7 天）——釣魚/撞庫常用新號，同樣行為更快升級
- 風險分數 = 殘留分數 +（訊號權重 + 累犯加成）× 信任係數——高信任者偶發行為更不易被處置

#### 🚨 Raid 精準偵測（不再誤報正常移入）
- 只統計**低齡帳號（<7 天）**在 60 秒內的湧入量（≥5 警示）
- 正常成員移入（好友邀請、伺服器搬遷）不再觸發假警報——比舊的「全量加入計數」更精準

#### 👥 跨使用者共識（連結信譽）
- **連結觀察池**：同一可疑連結被 ≥2 個「不同使用者」發送 → 判定多人共識
- 共識達成才在學習候選中加權（+1）——同一個人重複發送（誤發/刷屏）不構成信譽

#### 🔤 同形字混淆還原（更深的仿冒偵測）
- **deobfuscate**：把 leet 混淆字元還原（0→o、1→l、3→e、4→a、5→s、7→t、@→a、$→s、!→i）
- **d1sc0rd.com → discord.com**：混淆過的官方域名不再被「官方本身不算」豁免——還原後比對，判定仿冒
- 正常域名（my-fan-page.com）與官方域名（discord.com）維持不誤判

#### 🧪 測試
- 新增 tests/v036Trust.test.js（混淆 7 項＋信任 4 項＋風險加權 3 項＋Raid 3 項＋共識 4 項＋學習加權 2 項 共 23 項）
- 適配 v031（member stub 補帳號年齡）、v035（grab 起點改 deobfuscate）
- **15 套件全部通過**（v036 納入 npm test）

---

### V0.3.5（2026-09-09）— 智慧學習強化・仿冒偵測・累犯感知

#### 🧠 仿冒域名偵測（編輯距離＋品牌關鍵詞）
- **編輯距離 ≤2**：disc0rd.com、disc0rdapp.com 等與官方域名極相似的仿冒
- **品牌關鍵詞**：含官方品牌名（steam/discord/github/roblox 等 42 個）的網域判定仿冒——steamgift.net、discord-verify.com 命中
- **精準控制**：官方域名本身與子域不算（discord.com、discord.gg 不誤判）；「my-fan-page.com」這類不含品牌名的不誤判

#### 📈 詐騙候選加權學習（輕量規則式）
- **跨伺服器共識**：同一候選域名出現在 ≥2 個伺服器 → 加權 +2（多個獨立社群都中招＝高可信）
- **新帳號關聯**：低齡帳號（<7 天）命中 → 加權 +2（釣魚常開新號）
- **仿冒加成**：命中仿冒官方偵測 → 加權 +2
- **來源記錄**：候選域名記錄首次來源伺服器（sources），便於審計

#### 👮 累犯感知（風險評分加成）
- **isKnownOffender**：查 30 天內 logsBuffer 的 BAN 紀錄——有前科的使用者再次異常，風險分數 +1
- 配合 V0.3.4 半衰期演算法：累犯更快到達處置門檻，但不會一擊必殺（仍走漸進制）

#### 🧪 測試
- 新增 tests/v035SmartLearn.test.js（仿冒 7 項＋加權 5 項＋累犯 5 項＋基礎 5 項 共 22 項）
- 修正 v028/v031/v034 對新簽名的適配（recordScamCandidate 帶 meta、風險區塊注入 logsBuffer/isKnownOffender）
- **14 套件全部通過**（v035 納入 npm test）

---

### V0.3.4（2026-09-09）— 行為風險演算法・去敏化

#### 🧮 行為風險評分演算法（取代盲目計數）
- 不再「10 分鐘內湊滿 N 次就升級」——改為**訊號權重 × 時間半衰期**的風險累積
- **訊號權重**：明確惡意訊號重（惡意檔案 4、撞庫 4、詐騙 3），易誤判訊號輕（@mention 1、重複 1）
- **半衰期 10 分鐘**：異常影響力隨時間指數衰減——正常使用者偶發行為自然歸零，持續異常者分數指數累積
- **門檻**：<6 分警告 → ≥6 分禁言 1 小時 → ≥10 分封鎖
- 實例：2 次輕微異常（各 1 分）只會警告；洪水×3（6 分）禁言；惡意檔案＋詐騙＋洪水（9 分）逼近封鎖

#### 🧹 去敏化（移除過於敏感的偵測，降低警報疲勞）
- **文字釣魚**：移除「free nitro」「nitro gift」寬鬆關鍵字（玩家聊天常見）——只保留「命令式動詞＋贈禮」的明確釣魚（claim nitro、discord gift code）
- **命令注入**：`$(pwd)`、``code``（markdown 行內程式碼）等工程聊天不再誤判——$() 與反引號需含明確惡意命令（curl/wget/nc/bash/rm...）才命中
- **@mention**：起步門檻 3 → 5 人（訊息 @5 人才較異常）

#### 🧪 測試
- 更新 v031Escalation（風險衰減/權重門檻/降級 21 項）、v030Stability（去敏化後行為）
- 新增 tests/v034Desensitize.test.js（去敏化 14 項＋權重 7 項 共 21 項）；13 套件全部通過

---

### V0.3.3（2026-09-09）— 進階注入偵測・原型污染消毒・資源耗盡防禦

#### 💉 進階注入偵測（僅警示，不刪除不處罰）
- **SQL 注入**：恆真條件（OR 1=1）、UNION SELECT、破壞性語句（; DROP TABLE）、注釋繞過（--）
- **命令注入**：分號鏈接（; rm）、管道執行（| sh）、$() 執行、反引號執行
- **JS / 模板注入**：${process...} 惡意模板、{{config...}} 樣板注入
- **誤判控制**：只命中「明確惡意組合」，正常教學內容（SELECT * FROM users、${name} 模板字串）不會誤判

#### 🧬 原型污染消毒（底層資料防護）
- 所有 JSON 資料檔載入時遞迴剝離 `__proto__` / `constructor` / `prototype` 鍵
- 攻擊者若竄改資料檔寫入原型鍵，無法透過 JSON.parse 污染物件原型（注入防禦）

#### 💣 資源耗盡防禦（反 DoS）
- **超長訊息**（>4000 字元）：只警示，跳過重量級偵測（重複/洪水等），避免惡意長輸入耗盡 regex 預算
- **附件炸彈**（單則 >5 個附件）：警示，不處罰

#### ⚙️ 新開關
- 安全設定：`injectionDetection`（預設開）；警報設定：`contentAbuse`（預設開）

#### 🧪 測試
- 新增 tests/v033Injection.test.js（消毒 8 項 / 注入命中 10 項 / 誤判控制 4 項 / 超長保護 1 項 共 23 項）；12 套件全部通過

---

### V0.3.2（2026-09-09）— 全域黑名單管理員同意制・輸入層消毒

#### 🌐 全域黑名單不再自動加入——改為管理員同意制
- **原做法**：任一伺服器抓到人，自動加入全域黑名單（跨伺服器永久封鎖、僅開發者可解除）——若開發者失聯將無人能解
- **新流程**：
  1. 伺服器抓到人 → 先**本伺服器封鎖**
  2. 自動向該伺服器「警報頻道」發送 **🌐 全域黑名單提名**（含原因、伺服器、時效 24 小時）
  3. 僅**管理員或白名單**可按下「✅ 同意加入全域」或「❌ 拒絕」
  4. 同意 → 加入全域黑名單 → 立即掃描所有伺服器清除該成員；拒絕 → 僅本伺服器封鎖
- 已確認的黑名單（全域掃描、加入即查、面板手動新增）維持即時封鎖，不重複提名
- 同一伺服器 24 小時內不重複提名同一人

#### 🧼 輸入層消毒（防繞過）
- 訊息進入偵測前，先剝離零寬空格 / ZWJ / BOM / 軟連字號 / 雙向控制等隱形字元
- 攻擊者用隱形字元插入關鍵字（如 d​iscord）繞過偵測的手法失效

#### 📡 底層 REST 速率限制監控
- 監聽 discord.js REST 429 限流事件，記錄路由與重試時間至 crash.log（discord.js 已自動退避）

#### 🧪 測試
- 新增 tests/v032GlobalConsent.test.js（消毒 5 項 / 管理員權限 5 項 / 提名重複抑制 3 項 / banUser 提名行為 3 項 共 16 項）；11 套件全部通過

---

### V0.3.1（2026-09-09）— 高置信度漸進處置

#### 🧠 設計原則：不再「單一規則直接 Ban」
- 簡單規則容易誤判（例如訊息一次 @ 3 個人是正常協作，帳號新不代表是攻擊者）
- 改為**多重獨立訊號交叉驗證**：單一異常只是警告，多種獨立異常短時間內連續觸發才是高置信度訊號

#### ⚖️ 漸進式處置（警告 → 禁言 → 封鎖）
- **一般訊號**（止損 / @mention / 腳本 / SelfBot / 洪水 / 語音 / 邀請 / 重複 / Rich Presence）：
  - 第 1 次觸發 → 警告（禁言 10 分鐘）
  - 10 分鐘內第 2 次（**跨類型也累積**）→ 禁言 1 小時
  - 第 3 次 → 封鎖
- **強訊號**（詐騙連結 / XSS / 惡意檔案 / 撞庫——明確惡意、誤判率極低）→ 維持 2 次封鎖
- 跨類型累積：同一個人 10 分鐘內先發洪水、再被偵測腳本行為，兩者合併計算——比單一規則重複更有信心

#### 🛡️ 仍直接封鎖的情況（不誤傷正常使用者）
- 全域黑名單（人工確認的封鎖）——維持即時封鎖
- 管理員 / 白名單使用者——一律豁免

#### 🧪 測試
- 新增 tests/v031Escalation.test.js（跨類型累積 / 警告→禁言→封鎖 / 強訊號 2 次封鎖 14 項）；10 套件全部通過

---

### V0.3.0（2026-09-09）— 穩定性・相容性・輕量化

#### 🧭 路徑相容性（從任何目錄啟動皆穩定）
- 所有資料檔（blacklist.json / config.json / logs.json / crash.log / .env 檢查）一律以 __dirname 定位，不再依賴工作目錄——Windows 開發與香橙派 systemd 執行環境皆一致

#### 🛰️ 斷線偵測
- 監聽 Discord ShardDisconnect：斷線記錄至 crash.log 供追查（不退出，discord.js 自動重連）

#### 🧹 面板限流追蹤清理
- checkCmdRate 改存物件、cleanupTrackers 新增 cmd_ 分支（逾 10 秒清理）——追蹤表不再殘留

#### 🚨 大量加入防護（raid 偵測）
- 60 秒內 ≥5 個新成員加入即發送警報（僅警示不自動處理，避免誤傷正常加入）

#### 💬 純文字釣魚偵測
- 無連結的贈禮/驗證詐騙話術（free nitro、discord gift、verify your account 等）——僅警示不刪除

#### 📊 記憶體可觀測性
- 每 6 小時記錄 RSS/heap 至日誌，供每日維護比對記憶體趨勢；啟動自檢顯示版本/平台/Node/目錄

#### 🧪 測試
- 新增 tests/v030Stability.test.js（大量加入/限流清理/文字釣魚/路徑硬化 25 項）；9 套件全部通過

---

### V0.2.9（2026-09-09）— 極致安全（防繞過＋部署點硬化）

#### 🔍 URL 混淆還原（防釣魚繞過）
- 還原 hxxp://、[.]、(.)、{ . }、全形點、空格、零寬字元等混淆手法後再檢查，繞過手法無所遁形
- 空格分隔混淆（discord . com）僅在含釣魚關鍵字時偵測，避免誤判正常文字

#### 📎 短網址服務偵測
- 辨識 bit.ly、tinyurl.com、goo.gl 等 16 個短網址服務：無法確認真實目標時記錄並發送警示，不誤刪合法短連結

#### ⏳ 面板指令限流
- !章魚 面板每使用者 5 秒限 1 次，防管理指令被濫發

#### 🕸️ Webhook 名稱假冒偵測
- 偵測名稱仿冒 discord/steam/nitro 官方或贈禮關鍵字的 Webhook 並發送警報

#### 🏰 部署點安全硬化（香橙派）
- SSH：停用密碼登入、root 僅限金鑰（prohibit-password）、最大 3 次認證嘗試、閒置逾時
- 防火牆 ufw：預設拒絕所有流入，僅允許區域網路（192.168.0.0/24）SSH
- fail2ban：SSH 暴力破解自動封鎖
- 自動安全更新：unattended-upgrades 每日檢查安全更新
- systemd 服務隔離：NoNewPrivileges、PrivateTmp、ProtectSystem=full、RestrictSUIDSGID、RestrictRealtime、LockPersonality
- 記憶體上限：MemoryHigh=200M / MemoryMax=300M（與 03:00 隨機重啟互補，防記憶體溢出拖垮系統）
- 硬化腳本 harden_pi.sh（冪等、可重跑、套用前自動備份）

#### 🧪 測試
- 新增 tests/v029Security.test.js（混淆還原/短網址/限流/假冒偵測 36 項）；8 套件全部通過

---

### V0.2.8（2026-09-09）— 學習持久化與防駭強化

#### 🧠 詐騙學習候選池持久化（重啟不丟學習進度）
- 學習統計從記憶體移至 blacklist.json 的 scamCandidateStats：每日 03:00 隨機重啟後，未達標的學習候選（偽官方命中統計）不再歸零
- 每筆候選記錄首次/最後命中時間；命中 ≥3 次且非官方 → 自動提升為明確詐騙域名（學習後移除候選，避免重複學習）
- 候選老化：超過 72 小時未再命中的候選自動清除，防止誤判與無界累積（高記憶體水位時亦主動清理）

#### 🛡️ 域名格式驗證（防注入）
- 新增 isValidDomain：僅允許合法網域字元、強制含點、最長 253 字元，拒絕前導/尾隨連字號與連續句點
- 回報詐騙連結與新增域名均經格式驗證，防止注入怪異字串污染清單

#### 🧪 測試
- 新增 tests/v028Persistence.test.js（候選累積/老化清除/格式驗證/結構保險 28 項）；v027 測試更新為持久化契約，7 套件全部通過

---

### V0.2.7（2026-09-09）— 操作簡化與詐騙域名自主學習

#### 🎛️ 操作簡化（只留一個入口）
- 指令收斂：唯一入口 !章魚，其餘操作全部整合到控制面板 UI
- 緊急停機改為面板按鈕（僅開發者可見）：🛑 緊急停機
- 詐騙域名管理改為面板子頁：🌐 詐騙域名（回報/新增/刪除）
- 黑名單/白名單新增移除維持僅開發者權限（按鈕只對開發者顯示，互動層二次驗證）

#### 🧠 詐騙域名自主學習（輕量規則式，無需 ML）
- 面板「📩 回報詐騙連結」：貼上可疑連結即被機器人學習，官方網站（白名單）無法被加入
- 命中統計：自訂域名每次被偵測命中自動累計；偽官方域名進入學習池
- 每 24 小時學習回合：偽官方域名被命中 ≥3 次自動提升為明確詐騙域名；加入超過 14 天仍零命中的低置信域名自動移除
- 學習紀錄含來源（管理員回報/管理員新增/自主學習）、回報次數、命中次數，皆於面板顯示

#### 🧪 測試
- 新增 tests/v027Learning.test.js（自動提升/低置信移除/官方保護/學習池清空 17 項），連同既有測試共 6 套件全部通過

---

### V0.2.6（2026-09-09）— 極致防護（防駭客強化）

#### 🕵️ Token 與憑證防護
- Token 遮罩：任何控制台輸出、crash.log 中出現的 DISCORD_TOKEN 一律替換為遮罩，bot token 不會再因錯誤訊息外洩
- 單實例鎖：防止誤啟動第二個實例同時操作設定檔/黑名單造成衝突，重複啟動直接拒絕
- .env 權限檢查（Unix）：group/other 可讀時啟動即警告，提醒 chmod 600

#### 🤖 假冒機器人偵測
- 定期（啟動後 60 秒、每 6 小時、新伺服器加入時）掃描各伺服器，發現名稱含 OctopCutes 的其他機器人即記錄警報

#### 🌐 自訂詐騙域名
- 管理員可用『!章魚 加詐騙域名 <網域>』／『!章魚 刪詐騙域名 <網域>』擴充詐騙黑名單，與內建清單合併判定（格式自動驗證）

#### ⚡ 記憶體感知自動調節
- 每分鐘監測 RSS：超過 180MB 自動把掃描間隔從 30 分鐘縮短到 10 分鐘並強制清理追蹤器；超過 260MB 縮短到 6 分鐘；恢復正常自動回復

#### 📢 @everyone/@here 濫用防護（新增安全開關：mentionSpam）
- 60 秒內 ≥3 次 @everyone/@here 觸發：刪訊息＋禁言警告（管理員/白名單豁免，不直接封鎖）

#### ⚡ 加入即封鎖
- 新成員加入時立即檢查全域黑名單，命中直接封鎖，不必等 30 分鐘定時掃描

#### 🧪 測試
- 新增 tests/v026Security.test.js（token 遮罩、@everyone 追蹤與清理、自訂詐騙域名 18 項），連同既有測試全部通過

---

### V0.2.5（2026-09-09）— 安全強化與防駭客功能（力大磚飛）

#### 🎣 詐騙連結攔截（新增安全開關：scamLink）
- 內建常見詐騙／釣魚域名黑名單（discord-nitro、discordgift、steamgift、netflix-gift 等 26 組），命中即刪訊息＋警告，10 分鐘內累犯第 2 次自動封鎖
- 偽官方域名偵測：域名含 discord／steam／nitro 關鍵字但非官方域名（如 discord.com.evil.com）一律視為可疑
- IP 直連連結偵測（http://123.45.67.89/... 通常是惡意）
- 官方域名白名單（discord、GitHub、YouTube 等 50+ 組，含子域）確保不會誤封正常分享

#### 🔁 重複內容偵測（新增安全開關：duplicateSpam）
- 同一使用者 30 秒內發送 ≥5 條內容相同（前 100 字元）的訊息即觸發，警告後累犯封鎖，專打刷屏機器人

#### 🎭 假冒名稱偵測（新增安全開關：impersonation）
- 新成員名稱含 discord／steam／nitro ＋ 贈禮／官方關鍵字組合（如「discord nitro gift」）時發出警報

#### 🛑 緊急停機（僅開發者）
- 開發者輸入 !章魚 停止 可遠端關閉機器人（先寫出未落盤日誌再退出），機器人失控時可立即止血

#### 🛡️ 自我防護
- 設定檔損壞自動備份：config.json／blacklist.json／logs.json 若被竄改或手動誤改導致 JSON 損壞，自動備份為 .corrupt-<時間戳> 再以預設值重啟，不讓壞檔弄掛機器人
- blacklist.json 結構保險：即使 JSON 合法但結構不完整也不會崩潰
- 啟動前驗證 .env：缺少或長度異常的 DISCORD_TOKEN 直接明確報錯退出，不再無限重試
- 偵測機器人被移出伺服器（GuildDelete）並記錄；加入新伺服器（GuildCreate）時自動建立預設設定並立即套用全域黑名單

#### 🔒 操作安全
- 面板輸入互斥鎖：同一使用者同時只能有一個「等待輸入」流程，防止重複點擊按鈕導致多個 awaitMessages 疊加干擾
- 自動回應防注入：設定回應時禁止 @everyone／@here，回應長度上限 1900 字元

#### 🧪 測試
- 新增 tests/scamLink.test.js（URL 提取、詐騙／偽官方／IP 直連判定、假冒名稱 27 項）與 tests/duplicateSpam.test.js（重複觸發、窗口重置、cleanup 清理 9 項），連同既有測試全部通過

---

### V0.2.4（2026-09-08）

#### ⚡ 效能與穩定性優化
- 日誌改為「記憶體緩衝 + 防抖寫入」：不再每次動作都同步讀寫整份 `logs.json`，大幅減少 SD 卡寫入次數與事件迴圈阻塞；`SIGTERM`／`SIGINT`／例外發生時會強制寫出未落盤資料
- 黑名單改用記憶體快取，避免每次操作重複讀檔
- `scanAll` 加上重入鎖，防止 30 分鐘定時掃描與啟動掃描重疊
- 監聽 discord.js 的 `error`／`warn` 事件（未監聽的 `error` 事件會直接讓 Node 崩潰）
- RateLimit 豁免導覽按鈕（返回／刷新／翻頁），管理員翻頁不會被誤鎖
- 新增 `tests/scheduleSave.test.js`，與既有測試皆通過（含 2 萬條壓力測試）

#### 🔒 隱私與倉庫整理
- `config.json`／`blacklist.json`／`logs.json` 移出版本控制（執行期自動產生，避免公開倉庫洩漏伺服器 ID 與封鎖紀錄），新增 `*.example.json` 範本
- `package.json` 補上名稱、版本、`start`／`test` 腳本與 Node 版本要求

---

### V0.2.3（2026-09-08）

#### 🐛 修復記憶體洩漏（長時間運作記憶體持續增長）
- 追蹤器（trackers）原本只清理「間隔型」條目，`addStrike`（累犯）、`checkBruteForce`（面板冷卻）、`trackBehavior`（行為追蹤）、`checkRateLimit` 產生的條目會永久殘留，導致記憶體無限增長
- `cleanupTrackers` 現在會依各類型過期語義逐型清理，並以 2 萬條壓力測試驗證可全數清除

#### 🛡️ 防止隨機崩潰
- 新增全域錯誤處理（`unhandledRejection`／`uncaughtException`），不再因為單一 Promise 拒絕就靜默退出，並將錯誤寫入 `crash.log` 供追查
- 登入失敗改為 30 秒後自動重試，不再直接退出
- 修正 `!章魚` 指令與按鈕互動在特殊情境（無 member 物件）下可能拋例外導致崩潰的問題

#### 🔧 修正失效功能
- 修正 `autoDegrade`（自動降級）形同虛設的問題：錯誤計數與請求計數原本從未累加，系統永遠不會進入保護模式，現在會正確統計

#### ✨ 其他
- 修正 `ban()` 使用已棄用的 `deleteMessageDays` 參數（改為 `deleteMessageSeconds`）
- 補上倉庫遺漏的 `package.json`（新 clone 才能 `npm install`）與 `.gitignore`
- 清除設定檔中已移除的舊開關 `commandWhitelist` 殘留

---

### V0.2.2（2026-07-29）

#### 🐛 修復誤判/誤封問題
- XSS 偵測移除常見程式關鍵字誤判，只認真正注入樣式，並略過程式碼區塊
- 惡意檔案偵測移除 `.js` / `.jar`，避免誤封開發者/Minecraft社群正常分享
- 移除「新帳號 <3天 直接永久封鎖」規則，改為僅警報
- 惡意 Rich Presence 偵測比照 XSS 收斂樣式

#### 🛡️ 新增緩衝機制
- XSS、惡意 Rich Presence 由「一次觸發即永久封鎖」改為「10分鐘內累犯第2次才封鎖」，首犯僅刪訊息+禁言10分鐘+警告

#### 📋 補齊稽核缺口
- 撞庫攻擊、邀請濫用封鎖原本未寫入 `logs.json`，已修正統一透過 `banUser()` 記錄
- 黑名單新增來源伺服器、原因、時間戳中繼資料

#### 🔧 修正既有 Bug
- 修正安全設定面板因 Discord 5 列元件上限，導致最後兩項（角色鎖定、頻道監控）永遠無法透過按鈕切換的問題，改為分頁顯示
- 移除完全沒有作用（未被程式碼讀取）的「指令白名單」開關
- 修正 `rateLimit` / `autoDegrade` 開關無論開關狀態都強制執行、形同虛設的問題
- 啟用原本定義但從未被呼叫的暴力破解防護函式（重新命名為「面板冷卻」）

#### ✨ 新增
- 伺服器層級開關 `honorGlobalBlacklist`，可選擇不套用其他伺服器造成的黑名單封鎖

---

### V0.2.1（2026-07-22）
- 修復 `cleanupTrackers` 間歇性崩潰問題
- 加入防呆檢查，避免追蹤器資料為空時噴錯

### V0.2.0（2026-07-22）— 重大更新：完全重構為純防護系統
- 移除頻道保護功能，轉型為全方位自動安全防護系統
- 新增 22 項安全防護、9 項警報系統
- 新增控制面板：安全/警報獨立開關、黑白名單管理（僅開發者）、自動回應、設定匯出
- 新增 `blacklist.json`、`config.json`、`logs.json` 三份資料儲存

### V0.1.2（2026-07-21）
- 開發者專屬管理功能（黑白名單僅開發者可修改）
- 管理員操作日誌
- 白名單功能（使用者/角色不受保護）
- 防呆機制：Ban 權限檢查、管理員跳過、白名單跳過、機器人跳過、ID 格式驗證、操作超時取消

### V0.1.1（2026-07-21）
- 修復 `config.json` 讀取失敗導致保護設定無法持久化的問題
- 新增 `logs.json` 操作日誌
- 統一 `banUser()` 函數集中管理所有 Ban 邏輯

### V0.1.0（2026-07-21）— 初始版本
- 頻道保護、全域黑名單、止損機制、`!章魚` 控制面板、定期掃描、設定持久化

---

## :pray: 銘謝

- **Discord.js** — 提供強大的 Discord API 封裝
- **所有測試者** — 協助找出 bug 並提供反饋
- **開源社群** — 提供各種靈感與支援

機器人頭像來源：網路素材，若有侵權請告知，將立即撤下。

---

## :page_facing_up: 授權

本專案採用 MIT 授權條款 — 詳見 [LICENSE](LICENSE) 檔案

## :lock: 隱私

使用本機器人即表示您同意 [隱私條款](PRIVACY.md)

---

## :warning: 使用風險自負

> 本機器人為輔助管理工具，**不保證 100% 防呆**。
> 因操作不當、誤 Ban、設定錯誤或其他任何原因造成的損失，**開發者不承擔任何責任**。
> 建議正式上線前先在測試伺服器確認各項偵測規則的敏感度是否符合你的社群需求。

---

## :handshake: 貢獻

歡迎提交 Issue 和 Pull Request！

---

Made with :octopus: by octopodiformes